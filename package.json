{
  "name": "latoon",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun --filter '*' build",
    "test": "bun --filter '*' test",
    "dev": "bun --filter '*' dev",
    "start": "bun --filter '*' start"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "workspaces": [
    "packages/*"
  ],
  "peerDependencies": {
    "typescript": "^5"
  }
}
