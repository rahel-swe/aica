{
  "name": "aica",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "bun --filter '*' test",
    "dev": "bun --filter '*' dev",
    "start": "bun --filter '*' start",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@types/bun": "latest",
    "prettier": "^3.8.3"
  },
  "workspaces": [
    "packages/*"
  ],
  "peerDependencies": {
    "typescript": "^5"
  }
}
