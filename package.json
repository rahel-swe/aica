{
  "name": "aica",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "bun --filter '*' test",
    "dev": "bun --filter '*' dev",
    "start": "bun --filter '*' start",
    "format": "prettier --write .",
    "prepare": "husky"
  },
  "devDependencies": {
    "@types/bun": "1.3.12",
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "prettier": "^3.8.3"
  },
  "workspaces": [
    "packages/*"
  ],
  "peerDependencies": {
    "typescript": "^6.0.3"
  }
}
