You are generating a structured AICA roadmap.

Return valid JSON only.
Do not wrap the response in markdown.
Do not include commentary before or after the JSON.

Important rules:
- Use the pathway data as the source of truth.
- Respect the user's setup assessment when deciding pace, depth, sequencing, and time realism.
- Do not pretend a long regulated pathway can be completed inside the first roadmap window.
- For regulated or long pathways, focus the roadmap on the first action window and preparation steps.
- For portfolio or skill routes, focus on foundations, proof of skill, and entry positioning.
- Keep the roadmap practical, realistic, and globally honest.
- Avoid fake institutions, fake certifications, and fake local legal claims.
- Match the output to the roadmap model and types exactly.
- Include every required field from the schema, including phase status, step status, and step phaseId.
- Make steps belong to phases through phaseId.
- Return steps as a top-level array, not nested inside phases.
- Keep phase IDs and step IDs unique.
- Do not invent extra fields outside the schema.

Pathway:
{{pathway_json}}

Roadmap setup assessment:
{{setup_json}}

Recommendation context:
{{recommendation_json}}

Return JSON in this exact shape:
{
  "title": "string",
  "summary": "string",
  "phases": [
    {
      "id": "phase_1",
      "phase": "foundation",
      "title": "string",
      "objective": "string",
      "order": 1,
      "status": "pending"
    }
  ],
  "steps": [
    {
      "id": "step_1",
      "title": "string",
      "why": "string",
      "estimatedTime": "string",
      "difficulty": "easy | medium | hard",
      "prerequisites": ["string"],
      "resources": [
        {
          "title": "string",
          "url": "string",
          "type": "course | video | article | project | tool | other"
        }
      ],
      "evidenceOfCompletion": "string",
      "status": "pending",
      "order": 1,
      "phaseId": "phase_1"
    }
  ]
}

Requirements for phases:
- Return exactly 3 phases.
- Use realistic journey stages such as foundation, practice, transition, application, portfolio, or preparation, depending on the pathway.
- Each phase must have a clear objective.
- Orders must be sequential.
- Phase IDs must be unique.
- Each phase must include a status field.

Requirements for steps:
- Return 2 to 4 steps per phase.
- Each step must belong to one phase through phaseId.
- Step IDs must be unique.
- Orders must be sequential within each phase.
- Each step must include a status field.
- Keep the first phase simpler for beginners.
- Prefer lightweight or free-friendly actions for low budget or weak internet.
- If the user has no laptop, do not make laptop-dependent work an immediate requirement unless unavoidable.
- Every step title must name a concrete action and output.
- Avoid generic titles such as "Advance the phase", "Build progress", "Learn more", or "Check constraints".
- Each step must create visible evidence: a checklist, short note, comparison table, mini project, practice result, portfolio item, application draft, interview summary, or decision record.
- Use resources only when they are generally useful; do not add fake URLs.

Requirements for quality:
- Tailor difficulty and estimated time to the user's weekly time, constraints, and roadmap style.
- Use the pathway's duration profile and journey phases.
- For regulated or long pathways, make the first roadmap window small and realistic.
- For portfolio or skill routes, focus on foundations, proof of skill, and entry positioning.
- Make estimated times specific and realistic.
- Use plain, useful language.
- Keep the roadmap practical for global users.
