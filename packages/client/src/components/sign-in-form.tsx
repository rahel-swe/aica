import type { SignInFormType } from '@contracts/shared/types/auth-types';

const SignInForm = () => {
  const onSubmit = (data: SignInFormType) => {};

  return <div> SignInForm</div>;
};

export default SignInForm;
